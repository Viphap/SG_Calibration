# SG_Calibration
This code is to drop Temperature and Daily Cycle out of SG data to extract truly dynamic SG. 

Train the model to learn the scenarios of the data with notebook, save the model in keras file. 

Use the pretrained model to predict + PELT to correct the calibration. 
